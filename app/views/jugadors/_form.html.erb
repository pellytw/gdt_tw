<%= form_for(@jugador) do |f| %>
  <% if @jugador.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jugador.errors.count, "error") %> prohibited this jugador from being saved:</h2>

      <ul>
      <% @jugador.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombres %><br />
    <%= f.text_field :nombres %>
  </div>
  <div class="field">
    <%= f.label :club_id %><br />
    <%= f.collection_select(:club_id, Club.all, :id, :nombre) %>
  </div>
   <div class="field">
    <%= f.label :posicion_id %><br />
    <%= f.collection_select(:posicion_id, Posicion.all, :id, :nombre) %>
  </div>
   <div class="field">
    <%= f.check_box :disponible %><%= f.label :disponible %>
    
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
