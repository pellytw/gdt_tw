<form action="<%= url_for :controller =>  "equipos", :action => "agregar_jugadores" %>">

    <p class="lead">ARQUEROS</p>
    <ul>
    <% Jugador.where("posicion_id=? and disponible=?",  Posicion.where(:nombre => "ARQUERO").first.id, true).each do |j|%>
      <li>
        <input type="checkbox" name="<%= j.id %>"> <%= j %></input><br>
      </li>
    <% end %>
    </ul>
    <p class="lead">DEFENSORES CENTRALES</p>
    <ul>
    <% Jugador.where("posicion_id=? and disponible=?", Posicion.where(:nombre => "DEFENSOR CENTRAL").first.id, true).each do |j|%>
      <li>
        <input type="checkbox" name="<%= j.id %>"> <%= j %><br>
      </li>
    <% end %>
    </ul>
      <p class="lead">DEFENSORES LATERALES</p>
    <ul>
    <% Jugador.where("posicion_id=? and disponible=?", Posicion.where(:nombre => "DEFENSOR LATERAL").first.id, true).each do |j|%>
      <li>
        <input type="checkbox" name="<%= j.id %>"> <%= j %><br>
      </li>
    <% end %>
    </ul>
    <p class="lead">VOLANTES CENTRALES</p>
    <ul>
    <% Jugador.where("posicion_id=? and disponible=?",  Posicion.where(:nombre => "VOLANTE CENTRAL").first.id, true).each do |j|%>
      <li>
        <input type="checkbox" name="<%= j.id %>"> <%= j %><br>
      </li>
    <% end %>
    </ul>
    <p class="lead">VOLANTES LATERALES</p>
    <ul>
    <% Jugador.where("posicion_id=? and disponible=?",  Posicion.where(:nombre => "VOLANTE LATERAL").first.id, true).each do |j|%>
      <li>
        <input type="checkbox" name="<%= j.id %>"> <%= j %><br>
      </li>
    <% end %>
    </ul>
    <p class="lead">DELANTEROS</p>
    <ul>
    <% Jugador.where("posicion_id=? and disponible=?", Posicion.where(:nombre => "DELANTERO").first.id, true).each do |j|%>
      <li>
        <input type="checkbox" name="<%= j.id %>"> <%= j %><br>
      </li>
    <% end %>
    </ul>
    <input type="submit" name="cargar equipo" value="cargar equipo">
  </form>